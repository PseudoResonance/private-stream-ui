# Default values for private-stream-ui.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: ghcr.io/pseudoresonance/private-stream-ui
  # This sets the pull policy for images.
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""
  # Overrides the image tag and variant with the image digest.
  digest: ""

# This is for the secretes for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext:
  fsGroup: 2000

securityContext:
  capabilities:
    drop:
      - ALL
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  runAsUser: 1000

# This is for supplying the configuration file.
config:
  playerTitle: Live Video Player
  playerDescription: Video player frontend for MediaMTX
  themeColor: "#FF0000"
  serverBaseUrl: https://live.localhost
  providerBase: https://backend.localhost
  thumbnailHttpsPort: 443
  thumbnailFormat: "webp"
  webRtcPort: 9090
  hlsPort: 9191

oidc:
  enabled: false
  existingSecret:
  secretKey: auth.json
  # If an existing secret is not specified, the following config can optionally be used
  config:
    # Enable if using Auth0 as the provider
    auth0Logout: false
    # The callback URL will be at /callback by default
    baseURL: "https://example.com"
    # Ensure there is no trailing slash
    issuerBaseURL: "https://auth.example.com"
    clientID: "CLIENT ID HERE"
    clientSecret: "CLIENT SECRET HERE"
  # Config for the "secret" string, a long, randomly generated string kept locally by the application
  localSecret:
    existingSecret:
    # Key for the automatically generated secret
    secretGeneratedKey: localSecret
    # The value can be specified manually if an existing secret is not used
    value: ""

databaseConfig:
  host: postgresql
  port: 5432
  database: stream
  maxConnections: 5
  idleTimeout: 60
  maxLifetime: 0
  connectionTimeout: 10
  auth:
    enabled: true
    # A username/password can be provided directly here
    # If no credentials are provided but auth is enabled, a password will be randomly generated
    username: "stream"
    password: ""
    # Alternatively, an existing secret can be used to supply the credentials
    existingSecret: ""
    # The keys that stores the credentials
    usernameKey: "username"
    passwordKey: "password"

# Pass extra environment variables to the pod
extraEnv: []
  # - name: SOME_ENV
  #   value: "100"

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port: 80

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:
  httpGet:
    path: /healthz
    port: http
readinessProbe:
  httpGet:
    path: /healthz
    port: http

#This section is for setting up autoscaling more information can be found here: https://kubernetes.io/docs/concepts/workloads/autoscaling/
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}

# Additional templated objects can be passed here
extraObjects: {}
