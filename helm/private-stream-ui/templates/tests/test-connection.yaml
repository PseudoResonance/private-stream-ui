apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "private-stream-ui.fullname" . }}-test-connection"
  labels:
    {{- include "private-stream-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "private-stream-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
